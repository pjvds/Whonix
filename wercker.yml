box: wercker-labs/docker

build:
    steps:
        - install-packages:
            packages: git
        - script:
            name: whonix_build_both
            code: |
                sudo -E ./whonix_build_both
